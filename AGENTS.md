# scen_dev 개발 규칙

이 파일이 Codex·Claude Code·사람에게 공통인 프로젝트 규칙의 정본이다. 에이전트별 파일에 같은 규칙을 복제하지 않는다. NGIP와 LAI Finance의 공통 작업 원칙을 이 저장소에 맞게 적용한다.

## 시작과 범위

- 실질적인 작업 전 `docs/09-outlook-direction.md`와 `WORKLOG.md`를 읽는다. 이전 헌장과 충돌하면 09 문서의 최신 제품 방향을 따른다. 데이터·모델의 상세 계약은 해당 `docs/` 문서에서 확인한다.
- 지시의 빈칸은 저장소의 근거와 되돌리는 비용으로 판단한다. 싸고 근거가 있는 선택은 스스로 결정해 보고한다. 사용자만 아는 사업 판단이나 되돌리기 비싼 범위 확장은 선택지와 추천안을 제시한다. 확인하지 않은 사실은 미결로 남긴다.
- 명세도 가설이다. 코드·원천 데이터와 어긋나면 양쪽을 검증하고, 수정할 때 명세와 검증을 함께 고친다. 틀렸던 판단과 근거는 기록한다. 실증하지 않은 숫자를 합격 기준처럼 쓰지 않는다.
- 다른 저장소의 제품 코드·데이터·자격증명을 이 저장소로 복사하지 않는다. NGIP와의 접합은 승인된 계약과 접근 권한에 따른다. DB 접근은 `db.py`를 경유하고 운영 연결의 읽기 전용 강제를 해제하지 않는다.

## 협업과 작업 기록

- 여러 에이전트의 검토가 유익하면 사용자가 그 작업에 협업을 요청했을 때 역할을 나눈다. 특정 AI 도구를 필수로 만들지 않는다. 다른 사람이나 에이전트의 변경을 덮어쓰거나 되돌리지 않는다.
- `WORKLOG.md`가 열린 작업·현재 상태·보류한 결정의 정본이다. 작업 시작 시 읽고, 끝나면 완료 항목을 열린 표에서 빼며 중요한 결과와 검증을 `docs/WORKLOG_ARCHIVE.md`에 짧게 남긴다. 채팅 기록만을 인수인계로 쓰지 않는다.
- 다른 파일이 소유한 상수·상태·규칙을 주석이나 문서에 복사하지 말고 해당 파일을 가리킨다. 두 곳의 값이 반드시 일치해야 하면 실제 사용 경로를 검사하는 테스트를 둔다. 주석은 값의 반복보다 해석과 이유를 기록한다.
- 완료된 일회성 문서는 살아 있는 문서에 흡수하거나, 코드 옆에 결론을 옮기거나, 명시적인 동결 기록으로 보존한다. 폐기된 설계를 현재 지시처럼 읽히게 두지 않는다.

## 언어와 화면

- 제품 UI의 라벨·버튼·상태·오류·도움말은 영어로 쓴다. 원천 데이터의 사람 이름·파일명·원문은 출처를 표시하고 원문대로 보여줄 수 있다. 분석 문장은 독자의 언어 설정을 따를 수 있지만 화면 조작 문구는 항상 영어다.
- 코드 주석, 내부 문서, 작업 기록, 커밋 메시지는 한국어로 쓴다. 외부 전달 문서의 언어는 독자와 문서 목적에 맞춰 정한다.
- 새 화면·컨트롤은 기존 토큰과 구성요소를 먼저 확인하고 목업이나 구체적인 레이아웃을 검토한 뒤 만든다. 새 화면은 직각 모서리, 절제된 구분과 기존 팔레트를 기본으로 한다. 빈 상태에는 이유와 다음 행동을 보여준다.
- `Studio`라는 이름은 저장 데이터를 만들거나 편집하는 화면에만 쓴다. 읽기·분석 화면의 이름과 권한은 실제 동작에 맞춘다.

## 검증과 품질 래칫

- 변경한 모듈의 테스트를 실행하고, 적용 가능하면 전체 `python -m pytest -q`, `python scripts/ratchet.py`, `python scripts/security_source_gate.py --all`을 실행한다. 화면 변경은 해당 사용자 흐름을 로컬에서 확인한다. CI는 전체 테스트·컴파일·래칫·보안 게이트를 실행한다.
- `ratchet.toml`은 현재의 품질 기준선이다. 기준선은 저절로 올리지 않는다. 더 좋아진 값은 낮추고, 악화가 정말 필요한 경우 코드·문서·커밋에 구체적인 이유를 남긴다. 검사에 예외를 추가하기 전 실제 사용처와 탐지 범위를 확인한다. 검사기는 위반 표본을 잡는 테스트로 보호한다.
- 래칫의 통과는 보안 승인이나 설계 적합성의 증거가 아니다. 테스트는 실제 동작과 회귀 위험을 검증하며 구현을 그대로 따라 쓰는 검사를 만들지 않는다.

## 보안 점검

- 본사 자가점검 도구 실행 전 `python scripts/security_scan_preflight.py`로 `debug/` 잔여물과 중첩 Git 작업 트리를 확인한다. 실패하면 소유 작업과 사용처를 확인해 정리한 뒤 검사하며 제외 설정으로 숨기지 않는다.
- 커밋 전 `python scripts/security_source_gate.py --staged`를 실행한다. 로컬 훅은 `git config --local core.hooksPath .githooks`로 연결한다. CI는 전체 작성 소스와 변경분을 다시 검사한다.
- 빈 오류 처리, 동적 SQL처럼 확실한 위반은 수정한다. 맥락상 안전한 새 위험 API는 `scripts/security_reviewed.json`에 정확한 코드와 한국어 근거를 남긴다. 등록은 본사 도구의 오탐 판정이나 예외 승인이 아니다. 본사 도구의 정규식·판정 정본은 본사 배포물이며 결과와 미해결 사항은 `docs/SECURITY_SELFCHECK.md`에서 관리한다.
- 자격증명·토큰·개인 정보·운영 데이터·`.env`는 Git에 넣지 않는다. `git add` 뒤에는 staged 파일 목록을 확인한다. 로그와 디버그 출력에도 민감한 값을 남기지 않는다.
- 임시 이미지·로그·검사 산출물은 `debug/`에 만들고 자신이 만든 것만 검증 후 정리한다. 기존 파일이나 다른 작업의 파일을 추측으로 지우지 않는다. 별도 Git 작업 트리는 저장소 밖의 형제 경로에 둔다.

## 배포 승인

- 배포는 사용자가 대상과 범위를 확인하고 **명시적으로 승인한 뒤에만** 실행한다.
- 구조·설계 동의, 구현 요청, job으로 관리하자는 결정, 커밋 요청은 배포 승인이 아니다.
- 코드 작성·검증·배포안 준비까지 진행하고 실제 배포 실행은 별도 승인을 기다린다.
