# scen_dev 작업 기록

진행 중인 일과 결정의 정본이다. 제품 방향은 `docs/09-outlook-direction.md`, 개발 규칙은 `AGENTS.md`를 따른다. 완료한 작업의 원인·검증은 `docs/WORKLOG_ARCHIVE.md`에 기록한다.

## 진행 중

| ID | 작업 | 다음 판단 또는 완료 조건 |
|---|---|---|
| S-001 | OUTLOOK 첫 구현의 기능·통계 설계와 로컬 화면 | `docs/13-outlook-statistical-model-design.md` 이후 문서와 현재 `outlook/` 작업을 검증하고 범위를 확정한다. |

## 열린 결정

| ID | 결정할 것 | 근거와 필요한 입력 |
|---|---|---|
| D-001 | 첫 배포 대상·범위·시점 | 배포는 별도 명시적 승인 뒤에만 한다. 코드 검증은 승인 전까지 진행할 수 있다. |

## 현재 상태

- 수집·분석 코드와 데이터·모델 문서는 이 저장소에 있다. 최근 방향의 정본은 `docs/09-outlook-direction.md`다.
- 보안 소스 게이트·사전 점검은 `scripts/`에 있고 CI와 Git 훅에 연결된다. 품질 기준선은 `ratchet.toml`이다.
- 화면과 최신 설계 문서에는 병행 작업이 있다. 상태를 변경하기 전 `git status --short`로 다른 작업을 확인한다.

## 결정하지 않은 것

- 과거 헌장의 Regime·Transition·Scenario 전체 체계를 첫 구현의 필수 범위로 확정하지 않는다. 방향 변경의 근거는 `docs/00-charter-and-principles.md`와 `docs/09-outlook-direction.md`를 따른다.

## 갱신 규칙

- 새 작업과 결정에는 고정 ID를 준다. 완료하면 열린 표에서 제거하고 필요한 결과를 아카이브로 옮긴다. ID는 재사용하지 않는다.
- 다른 파일의 값이나 상태를 베끼지 말고 그 파일을 가리킨다. 검증하지 않은 숫자는 사실처럼 적지 않는다.
- 타임라인은 기능이나 운영 경계가 바뀐 마일스톤에만 둔다. 작은 수정의 기록은 아카이브에 둔다.
